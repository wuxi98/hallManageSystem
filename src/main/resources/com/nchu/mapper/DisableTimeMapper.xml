<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nchu.mapper.DisableTimeMapper">
    <insert id="insert">
        insert into disable_time
        value (#{hallId},#{day},#{startTime},#{content},#{endTime})
    </insert>
</mapper>